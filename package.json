{
  "name": "pulseops",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "web",
    "server",
    "agent"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "dev:agent": "npm run dev --workspace agent",
    "build": "npm run build --workspace web && npm run build --workspace server && npm run build --workspace agent",
    "lint": "npm run lint --workspace web && npm run lint --workspace server && npm run lint --workspace agent"
  }
}
